{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "InterviewObserverOutput",
  "type": "object",
  "properties": {
    "status": {
      "type": "string",
      "enum": ["on_track", "drifting", "stuck", "off_track"],
      "description": "Current state of candidate progress relative to the problem and reference solution."
    },
    "confidence": {
      "type": "number",
      "minimum": 0.0,
      "maximum": 1.0,
      "description": "Confidence score in this assessment."
    },
    "evidence": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Specific evidence observations from diagram digests, code snapshots, or recent transcript turns."
    },
    "missing_topics": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Critical architectural pillars or algorithmic requirements currently omitted by candidate."
    },
    "suggested_probe": {
      "type": "string",
      "description": "Short, natural, non-revealing probing question for the interviewer to ask."
    }
  },
  "required": ["status", "confidence", "evidence", "missing_topics", "suggested_probe"],
  "additionalProperties": false
}
